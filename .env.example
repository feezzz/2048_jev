# Jev（TypeSafe System One）API key
#
# 获取方式：https://console.typesafe.ai 创建，类型类似 sk-...
#
# 用法：把本文件复制成 .env，再把 key 填进去
#   cp .env.example .env
#
# 说明：这个 key 只在 Vite dev server 进程里读取，
#       请求时代理层注入 Authorization 头，
#       不会被打进前端产物，也不会出现在浏览器里。
TYPESAFE_API_KEY=
